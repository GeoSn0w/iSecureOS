# Uncomment the next line to define a global platform for your project
# platform :ios, '11.0'

target 'iSecureOS' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for iSecureOS
pod "AWFileHash"
  target 'iSecureOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'iSecureOSUITests' do
    # Pods for testing
  end

end
